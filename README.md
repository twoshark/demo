# C# in Docker Example

# Running Locally

```
dotnet run .
```

# Docker Build

```
docker build . --tag twoshark/docker-cs-demo --no-cache --progress plain
```

# Run with Docker Compose
```
docker-compose up
```